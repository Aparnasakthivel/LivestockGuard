{
  "name": "livestockguard-ai",
  "private": true,
  "scripts": {
    "start:frontend": "cd frontend && npm install && npm run dev",
    "start:backend": "cd backend && npm install && npm run dev",
    "start:ai": "cd ai-service && pip install -r requirements.txt && uvicorn main:app --reload --host 0.0.0.0 --port 8001"
  }
}
